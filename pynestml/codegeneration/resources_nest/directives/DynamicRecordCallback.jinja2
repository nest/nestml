{#
  Registers recordables in DynamicRecordablesMap for recording
  @param variable VariableSymbol
-#}
{%- set varDomain = declarations.get_domain_from_type(variable.get_type_symbol()) %}
{%- if varDomain == "double" and variable.is_recordable and not variable.has_vector_parameter() %}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */{% endif %}
insert("{{variable.get_symbol_name()}}", host.get_data_access_functor( {{neuronName}}::State_::{{names.name(variable).upper()}} ));
{%- endif %}
