{#
  Generates C++ declaration
  @param ast ASTFunctionCall
#}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
{%- if utils.is_integrate(ast) -%}
{%- if tracing %}/*      ----- start integration step (analytic) -----      */ {% endif %}
{%- include "directives/AnalyticIntegrationStep.jinja2" -%}
{%- if tracing %}/*      ----- start integration step (GSL) -----      */ {% endif %}
{%- include "directives/GSLIntegrationStep.jinja2" -%}
{%- if tracing %}/*      ----- end integration step -----      */ {% endif %}
{%- else -%}
{{printer.print_method_call(ast)}};
{%- endif -%}
