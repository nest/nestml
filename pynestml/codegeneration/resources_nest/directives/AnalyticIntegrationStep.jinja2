{#
  Generates a series of C++ statements which perform one integration step of all ODEs that are solved by the analytic integrator.
#}
{% if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
{% if uses_analytic_solver -%}
{   // N.B. declaring the "__tmp" variables in a local scope to avoid multiple definitions
{% for variable, update_expression in update_expressions.items() -%}
    double {{variable}}__tmp = {{printer.print_expression(update_expression)}};
{% endfor -%}
{% for variable, update_expression in update_expressions.items() -%}
    {{variable}} = {{names.name(variable)}}__tmp;
{% endfor -%}
}
{% endif -%}
