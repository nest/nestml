{#
  Assigns a tmp value which was read from the dictionary to the corresponding block variable.

  @param variable VariableSymbol
  @result C++ Block
#}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
{%- if not variable.is_inline_expression %}
{%-   if not variable.is_state() %}
{{names.setter(variable)}}(tmp_{{names.name(variable)}});
{%-   else %}
{{names.setter(variable)}}(tmp_{{names.convert_to_cpp_name(variable.get_symbol_name())}});
{%-   endif %}
{%- else %}
// ignores '{{names.name(variable)}}' {{declarations.print_variable_type(variable)}}' since it is an function and setter isn't defined
{%- endif %}
