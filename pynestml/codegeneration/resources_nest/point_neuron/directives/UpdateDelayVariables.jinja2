{#
  Generates C++ statements that update the delay variables
  @param variable VariableSymbol
#}
    delayed_{{variable.get_symbol_name()}} [delayed_{{variable.get_symbol_name()}}_idx] = {{printer.print_origin(variable)}}{{names
    .name(variable)}};
    delayed_{{variable.get_symbol_name()}}_idx = (delayed_{{variable.get_symbol_name()}}_idx + 1) % delay_{{variable.get_symbol_name()}}_steps;