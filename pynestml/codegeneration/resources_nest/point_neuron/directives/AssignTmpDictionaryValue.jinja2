{#
  Assigns a tmp value which was read from the dictionary to the corresponding block variable.

  @param variable VariableSymbol
  @result C++ Block
#}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
{%- if not variable.is_inline_expression %}
{{ cpp_setter_printer.print(utils.get_state_variable_by_name(astnode, astnode.get_state_blocks(), variable)) }}(tmp_{{ printer.print(utils.get_state_variable_by_name(astnode, variable)) }});
{%- endif %}
