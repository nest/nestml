{#
  Generates an instruction to write a value into the dictionary of registered variables.
  @param variable VariableSymbol
#}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
{% if not variable.is_internals() -%}
  def<{{declarations.print_variable_type(variable)}}>(__d, "{{variable.get_symbol_name()}}", {{names.getter(variable)}}());
{%- endif -%}