{#
  Generates a series of C++ statements which perform one integration step of
  all odes defined the neuron.
#}
{%- if tracing %}/* generated by {{self._TemplateReference__context.name}} */ {% endif %}
y0 = Neuron_{{neuronName}}.State_.to_vector()
res = scipy.integrate.solve_ivp(Neuron_{{neuronName}}.dynamics, y0, np.linspace(0., 351., 100), args=(self.neuron))
Neuron_{{neuronName}}.State_.from_vector(res.y[:, -1])