neuron invalidExpressionTest:
  state:
    V1,V2,V3 V
    V4 V
    A1 mA
    R1 Ohm
  end

  function foo(amp A, ohm Ohm) V:  
    return 7                    #2Warnings: Wrong return type AND conversion error
  end

  input:
      spikeInh   <- inhibitory spike
      spikeExc   <- excitatory spike
      currents <- current
  end

  parameter:
    
  end

  output: spike

  internal:
  end

  update:
    B boolean = A1 and R1 #Error: Operands not boolean.
    
    if R1 > 0:                  #Warning: Comparison of Unit and primitive 
    elif A1 == R1:              #Warning: Compared unitTypes do not match
    elif 0 < R1:                #Warning: Comparison of primitive and unit
    end
    
    V4 = V1+V2?2V:0V            #Error: first expression ist not a condition.
    V4 = A1>3mA?2:1Ohm          #Warning: Mismatched Types in conditional alternatives.

    V1 = 2*"stringvalue"        #Error
    
  end
end
