component CompMembers:
    state:
        alias foo real = 13 * test # error 1 test is internal variable
        bar real = test + 1.1 * P2 # error 2 test is internal variable

    end

    internals:
        test real = 13
    end

    parameters:
        P1 real = 2 + P2 # error: P2 is undefined at this place
        P2 real = 2 * P1 + P2 # P3 is undefined at this place
    end
end
