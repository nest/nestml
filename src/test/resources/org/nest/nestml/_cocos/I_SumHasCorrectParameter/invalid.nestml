neuron invalid_I_sum:
  state:
    V_m mV
    G pA
  end

  equations:
    G = t
    V_m' = V_m+I_sum(G*2, spikes)
    V_m' = V_m+I_sum(G, spikes*2)
    V_m' = V_m+I_sum(G+G, spikes)
  end

  input:
    spikes   <- spike
    currents <- current
  end
end