neuron NeuronInTest:
    state:
        x mV
        g_z mV
    end

    equations:
      x mV = 1
      g_z'' = t
      g_z'' = t*2
    end

    parameter:
        x real = x * 2
        y real
    end

    input:
       y <- spike
    end

    equations:
       x real = 12
    end
end

