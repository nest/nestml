neuron invalid_curr_sum:
  state:
    V_m mV
    G pA
  end

  equations:
    V_m' = V_m+convolve(G*2, spikes) # error1: G*2 is false
    V_m' = V_m+convolve(G, spikes*2) # error2: spikes*2 is false
    V_m' = V_m+convolve(G+G, spikes) # error3: G+G is false
  end

  input:
    spikes nS <- spike
    currents <- current
  end
end