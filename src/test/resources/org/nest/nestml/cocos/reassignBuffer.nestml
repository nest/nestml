package useAliasInNonAliasDecl.nestml.reassignBuffer:
    neuron ReassignBuffer:
        input:
            testBuffer <- spike
        end
        
        dynamics timestep(t ms):
            testBuffer = 15
        end
    end
