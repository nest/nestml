package org.nest.nestml.parsing.neuronWithUse:

    component EmptyComponent:


    end

    neuron AliasNeuron:
        use EmptyComponent as Soma
        
        dynamics timestep(t ms):
        end

        input:
          spikeBuffer   <- inhibitory excitatory spike
          currentBuffer <- current
        end

        output: spike
    end
