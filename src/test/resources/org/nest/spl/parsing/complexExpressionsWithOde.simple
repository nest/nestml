module org.nest.spl.parsing.complexExpressionsWithOde
tau_in real
t real
C_m real
I real
w real
V real
Tau real

ODE:
    I := w * (e/tau_in) * t * exp(-1/tau_in*t)
    d/dt V := -1/Tau * V + 1/C_m*I
end
