module org.nest.spl._cocos.codeAfterEmbeddedReturningIf

x, y real
x = 1
y = 2
while x <= 10 :
    if x > 4:
        return x
    else:
        return y
    end
    
    y = x*2
    x = x+1
end

print("x = " + x)